[project]
name = "circular-agents"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["circular-events"]

[tool.uv.sources]
circular-events = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/circular"]
