[project]
name = "circular-fake-agent-workload"
version = "0.1.0"
requires-python = ">=3.12"

[project.scripts]
circular-fake-agent-workload = "circular.fake_agent_workload.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/circular"]
