{
  "name": "circular",
  "private": true,
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "contracts:generate": "node scripts/contracts.mjs",
    "contracts:check": "node scripts/contracts.mjs --check",
    "test:e2e": "playwright test",
    "dev": "corepack pnpm --filter @circular/web dev",
    "build": "corepack pnpm --filter @circular/web build",
    "test": "corepack pnpm --filter @circular/web test",
    "typecheck": "corepack pnpm --filter @circular/web typecheck"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "openapi-typescript": "^7.13.0"
  }
}
